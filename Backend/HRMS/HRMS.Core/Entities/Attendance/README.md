# 📦 Domain Entities - HR_ATTENDANCE Schema

## ✅ الكيانات المنشأة

تم إنشاء جميع كيانات HR_ATTENDANCE بنجاح مع التوثيق الكامل بالعربية.

---

## 📋 قائمة الكيانات

### **1. ShiftType** (أنواع المناوبات)
**المسار:** `HRMS.Core/Entities/Attendance/ShiftType.cs`  
**الجدول:** `HR_ATTENDANCE.SHIFT_TYPES`

**الوصف:** تعريف أوقات الورديات (صباحي، مسائي) وعدد ساعاتها.

**الحقول الرئيسية:**
- `ShiftNameAr` - اسم المناوبة
- `StartTime`, `EndTime` - أوقات العمل
- `HoursCount` - عدد الساعات
- `IsCrossDay` - هل تمتد لليوم التالي

---

### **2. RosterPeriod** (فترات الجداول)
**المسار:** `HRMS.Core/Entities/Attendance/RosterPeriod.cs`  
**الجدول:** `HR_ATTENDANCE.ROSTER_PERIODS`

**الوصف:** تحديد الفترات الزمنية (شهر/سنة) لإعداد الجداول.

**الحقول الرئيسية:**
- `Year`, `Month` - الفترة
- `StartDate`, `EndDate` - التاريخ
- `IsLocked` - هل الفترة مغلقة

---

### **3. EmployeeRoster** (جدول الموظف)
**المسار:** `HRMS.Core/Entities/Attendance/EmployeeRoster.cs`  
**الجدول:** `HR_ATTENDANCE.EMPLOYEE_ROSTERS`

**الوصف:** توزيع الموظفين على المناوبات في أيام محددة.

**الحقول الرئيسية:**
- `EmployeeId` (FK) - الموظف
- `RosterDate` - التاريخ
- `ShiftId` (FK) - المناوبة المخططة
- `IsOffDay` - هل هو يوم راحة

---

### **4. RawPunchLog** (سجلات البصمة)
**المسار:** `HRMS.Core/Entities/Attendance/RawPunchLog.cs`  
**الجدول:** `HR_ATTENDANCE.RAW_PUNCH_LOGS`

**الوصف:** البيانات الخام الواردة من أجهزة البصمة.

**الحقول الرئيسية:**
- `DeviceId` - جهاز البصمة
- `PunchTime` - وقت البصمة
- `PunchType` - نوعها (دخول/خروج)

---

### **5. DailyAttendance** (الحضور اليومي)
**المسار:** `HRMS.Core/Entities/Attendance/DailyAttendance.cs`  
**الجدول:** `HR_ATTENDANCE.DAILY_ATTENDANCE`

**الوصف:** السجل النهائي للحضور بعد المعالجة والمقارنة مع الجدول.

**الحقول الرئيسية:**
- `ActualInTime`, `ActualOutTime` - الوقت الفعلي
- `LateMinutes` - التأخير
- `OvertimeMinutes` - الإضافي
- `Status` - الحالة (حاضر، غائب، إجازة)

---

### **6. ShiftSwapRequest** (طلبات التبديل)
**المسار:** `HRMS.Core/Entities/Attendance/ShiftSwapRequest.cs`  
**الجدول:** `HR_ATTENDANCE.SHIFT_SWAP_REQUESTS`

**الوصف:** طلبات الموظفين لتبادل المناوبات.

**العلاقات:**
- `Requester` → مقدم الطلب
- `TargetEmployee` → الموظف البديل

---

### **7. OvertimeRequest** (طلبات الإضافي)
**المسار:** `HRMS.Core/Entities/Attendance/OvertimeRequest.cs`  
**الجدول:** `HR_ATTENDANCE.OVERTIME_REQUESTS`

**الوصف:** طلبات الموافقة على العمل الإضافي.

**الحقول الرئيسية:**
- `HoursRequested` - الساعات المطلوبة
- `ApprovedHours` - الساعات المعتمدة
- `Status` - حالة الطلب

---

### **8. AttendancePolicy** (سياسات الحضور)
**المسار:** `HRMS.Core/Entities/Attendance/AttendancePolicy.cs`  
**الجدول:** `HR_ATTENDANCE.ATTENDANCE_POLICIES`

**الوصف:** إعدادات وقواعد الحضور (التأخير المسموح، معامل الإضافي).

---

## 📊 العلاقات والترابط

1. **الربط مع الموظفين**: جميع الكيانات مرتبطة بـ `EmployeeId` من `HR_PERSONNEL`.
2. **الربط مع الهيكل التنظيمي**: السياسات مرتبطة بـ `DeptId` و `JobId` من `HR_CORE`.
3. **التكامل**:
   - `EmployeeRoster` يربط الموظف بـ `ShiftType`.
   - `DailyAttendance` يقارن البيانات من `RawPunchLog` مع `EmployeeRoster`.
