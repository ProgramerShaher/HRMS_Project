# 🛠️ دليل حل مشاكل الاتصال بقاعدة البيانات (Oracle Database Troubleshooting)

هذا الملخص يوضح الخطوات التي قمنا بها لحل مشاكل الاتصال، ليكون مرجعاً لك في المستقبل.

---

## 1. المشكلة الأولى: توقف الخدمة (ORA-12541)
**الرسالة:** `ORA-12541: Cannot connect. No listener at host localhost port 1521`  
**السبب:** خدمة "المستمع" (Listener) المسؤولة عن استقبال الاتصالات في الويندوز كانت إما متوقفة أو لم تعمل تلقائياً.

### ✅ الحل:
تشغيل الخدمات يدوياً عبر **PowerShell** (كمسؤول) أو عبر تطبيق **Services** في الويندوز.

**الأوامر:**
```powershell
net start OracleOraDB21Home1TNSListener
net start OracleServiceXE
```

---

## 2. المشكلة الثانية: إعدادات الاتصال الخاطئة (SID vs Service Name)
**السبب:** المحاولة الاتصال باستخدام `SID: xe` بدلاً من `Service Name: xepdb1`.
المستخدمين (مثل `HR_CORE`) موجودين داخل الحاوية الإضافية (PDB) وليس الحاوية الرئيسية.

### ✅ الإعدادات الصحيحة (في SQL Developer أو VS Code):

| الإعداد | القيمة الصحيحة | ملاحظة هامة |
| :--- | :--- | :--- |
| **Connection Type** | `Basic` | |
| **Hostname** | `localhost` | |
| **Port** | `1521` | |
| **Service Name** | `xepdb1` | ⚠️ **لا تستخدم SID**، ولا تتركها `xe`. يجب أن تكون `xepdb1`. |
| **SID** | *(اتركه فارغاً)* | |

---

## 3. المشكلة الثالثة: خطأ في اسم المستخدم أو كلمة المرور (ORA-01017)
**الرسالة:** `ORA-01017: invalid username/password; logon denied`
**السبب:** إما كلمة المرور خاطئة، أو (وهو الأغلب) أنك تحاول الاتصال بـ `xe`، وقاعدة البيانات لا تجد المستخدم هناك فتعطيك خطأ "بيانات خاطئة".

### ✅ الحل:
1. تأكد أنك تتصل بـ `Service Name: xepdb1`.
2. استخدم بيانات الدخول المعتمدة في المشروع:

| المستخدم (User) | كلمة المرور (Password) |
| :--- | :--- |
| **HR_CORE** | `Pwd_Core_123` |
| **HR_PERSONNEL** | `Pwd_Personnel_123` |
| **HR_ATTENDANCE** | `Pwd_Attend_123` |
| **HR_PAYROLL** | `Pwd_Payroll_123` |
| **HR_LEAVES** | `Pwd_Leaves_123` |
| **HR_RECRUITMENT** | `Pwd_Recruit_123` |
| **HR_PERFORMANCE** | `Pwd_Perform_123` |

---

## 📝 ملخص سريع (Checklist)
قبل بدء العمل، تأكد من:
1. [ ] تشغيل خدمة `OracleOraDB21Home1TNSListener`.
2. [ ] تشغيل خدمة `OracleServiceXE`.
3. [ ] التأكد أن إعداد الاتصال يستخدم **Service Name: xepdb1**.



# إيقاف
net stop OracleServiceXE
net stop OracleOraDB21Home1TNSListener

# تشغيل
net start OracleServiceXE
net start OracleOraDB21Home1TNSListener

// Restart-Service -Name "OracleServiceXE","OracleOraDB21Home1TNSListener" -Force